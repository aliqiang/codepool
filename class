-- 普通类
class p(a:Int,b:Int){
   
}

--object类
单例对象，相当于java的static类。成员变量可以直接使用类名称调用
object p{
   
}


--案例类
和普通类没有多大区别，但是有几个关键特性：1.生成实例时不需要使用new，2.可以直接做对象比较，比较的是对象的成员变量，而不是引用
case class p(a:Int,b:Int) {
}

--特质类
相当于java的接口
trait p(){
  def f():Unit;
}
class sub extends p{
overwride f():Unit{println("xx")}
}
